<%= form_for(@link) do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task_id %><br>
    <%= f.number_field :task_id %>
  </div>
  <div class="field">
    <%= f.label :link_description %><br>
    <%= f.text_field :link_description %>
  </div>
  <div class="field">
    <%= f.label :link_src %><br>
    <%= f.text_field :link_src %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
