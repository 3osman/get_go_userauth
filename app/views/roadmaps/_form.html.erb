<%= form_for(@roadmap) do |f| %>
  <% if @roadmap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@roadmap.errors.count, "error") %> prohibited this roadmap from being saved:</h2>

      <ul>
      <% @roadmap.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating %>
  </div>
  <div class="field">
    <%= f.label :country %><br>
    <%= f.text_field :country %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
